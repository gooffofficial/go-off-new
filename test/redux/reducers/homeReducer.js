const initState = {
    modalActive: false,
    modalType: null,
}

export const homeReducer = (state = initState, action) => {

    switch (action.type) {
        
        default:
            return state
    }
}